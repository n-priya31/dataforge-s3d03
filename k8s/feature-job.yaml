apiVersion: batch/v1
kind: Job
metadata:
  name: job-03-feature
  labels:
    app: dataforge
    job: feature-engineering
spec:
  template:
    metadata:
      labels:
        app: dataforge
        job: feature-engineering
    spec:
      containers:
        - name: feature-engineering
          image: dataforge-feature:local
          imagePullPolicy: Never
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"
          volumeMounts:
            - name: shared-data
              mountPath: /data
      restartPolicy: Never
      volumes:
        - name: shared-data
          persistentVolumeClaim:
            claimName: dataforge-shared-data
  backoffLimit: 2
